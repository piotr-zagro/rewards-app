# Rewards App <a name="top"></a>

## Launch instruction

Application will run without any additional dependencies. UserIds required to perform api calls are in `application.yaml` under the `userIds:` property. 